# SimpleConsoleApp

A simple .NET console application that prints a greeting.

## Run

```bash
dotnet run --project SimpleConsoleApp/SimpleConsoleApp.csproj
```

Optionally pass a name:

```bash
dotnet run --project SimpleConsoleApp/SimpleConsoleApp.csproj -- Ada Lovelace
```
